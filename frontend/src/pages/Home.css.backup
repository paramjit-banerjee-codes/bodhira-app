/* ==================== HOME PAGE - WORLD-CLASS PREMIUM DESIGN ==================== */

* {
  box-sizing: border-box;
}

.home-container {
  min-height: 100vh;
  background: linear-gradient(180deg, 
    #0a0e27 0%, 
    #0f1429 25%, 
    #141a2f 50%, 
    #0f1429 75%, 
    #0a0e27 100%);
  position: relative;
  overflow: hidden;
  font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', sans-serif;
}

/* ==================== HERO SECTION - TWO COLUMN LAYOUT ==================== */

.hero-section {
  min-height: 100vh;
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 60px;
  align-items: center;
  justify-items: center;
  padding: 0px 60px 80px 60px;
  max-width: 1400px;
  margin: 0 auto;
  position: relative;
  z-index: 2;
}

/* Left Column - Text & CTA */
.hero-left {
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: flex-start;
  z-index: 10;
  width: 100%;
  max-width: 550px;
}

.hero-content {
  animation: fadeInUp 0.8s ease-out;
  width: 100%;
}

/* Badge */
.hero-badge {
  display: inline-flex;
  align-items: center;
  gap: 10px;
  padding: 10px 18px;
  background: rgba(30, 58, 138, 0.12);
  border: 1px solid rgba(30, 58, 138, 0.25);
  border-radius: 20px;
  margin-bottom: 32px;
  width: fit-content;
  animation: slideInDown 0.6s ease-out;
}

.badge-dot {
  width: 6px;
  height: 6px;
  background: #60a5fa;
  border-radius: 50%;
  animation: pulse 2s ease-in-out infinite;
}

.badge-text {
  font-size: 13px;
  color: #e2e8f0;
  font-weight: 600;
  letter-spacing: 0.3px;
}

@keyframes pulse {
  0%, 100% { opacity: 1; }
  50% { opacity: 0.6; }
}

/* Hero Title */
.hero-title {
  font-size: 60px;
  font-weight: 700;
  line-height: 1.15;
  color: #f1f5f9;
  margin: 0 0 24px 0;
  letter-spacing: -1px;
  animation: slideInUp 0.7s ease-out 0.1s both;
}

.gradient-text {
  background: linear-gradient(135deg, #60a5fa 0%, #93c5fd 100%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
}

/* Subtitle */
.hero-subtitle {
  font-size: 18px;
  color: #cbd5e1;
  line-height: 1.5;
  margin: 0 0 80px 0;
  max-width: 520px;
  font-weight: 400;
  animation: slideInUp 0.7s ease-out 0.2s both;
}

/* CTA Buttons */
.hero-cta-buttons {
  display: flex;
  gap: 16px;
  flex-wrap: wrap;
  margin-top: 28px;
  margin-bottom: 24px;
  animation: slideInUp 0.7s ease-out 0.3s both;
}

.cta-button {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: 10px;
  padding: 14px 28px;
  border-radius: 10px;
  font-size: 15px;
  font-weight: 600;
  text-decoration: none;
  cursor: pointer;
  transition: all 0.3s cubic-bezier(0.34, 1.56, 0.64, 1);
  white-space: nowrap;
  border: 1px solid transparent;
}

.cta-primary {
  background: linear-gradient(135deg, #1e3a8a 0%, #1e40af 100%);
  color: white;
  box-shadow: 0 12px 32px rgba(30, 58, 138, 0.25);
}

.cta-primary:hover {
  transform: translateY(-4px);
  box-shadow: 0 20px 48px rgba(30, 58, 138, 0.35);
  background: linear-gradient(135deg, #1e40af 0%, #3b82f6 100%);
}

.cta-secondary {
  background: rgba(255, 255, 255, 0.04);
  color: #e2e8f0;
  border: 1px solid rgba(255, 255, 255, 0.1);
}

.cta-secondary:hover {
  background: rgba(255, 255, 255, 0.08);
  border-color: rgba(255, 255, 255, 0.2);
  color: #f1f5f9;
}

.cta-arrow {
  font-weight: 700;
}

/* Subtext */
.hero-subtext {
  font-size: 13px;
  color: #94a3b8;
  margin: 0;
  animation: slideInUp 0.7s ease-out 0.4s both;
}

/* Right Column - Visual/Illustration */
.hero-right {
  display: flex;
  align-items: center;
  justify-content: center;
  position: relative;
  width: 100%;
  height: 550px;
}

/* ==================== ABOUT BODHIRA CARD ==================== */

.about-bodhira-card {
  width: 100%;
  max-width: 450px;
  height: auto;
  min-height: 400px;
  display: flex;
  flex-direction: column;
  padding: 56px;
  background: linear-gradient(135deg, 
    rgba(15, 23, 42, 0.8) 0%, 
    rgba(30, 58, 138, 0.4) 50%,
    rgba(15, 23, 42, 0.8) 100%);
  border: 1px solid rgba(96, 165, 250, 0.25);
  border-radius: 28px;
  cursor: pointer;
  transition: all 0.5s cubic-bezier(0.34, 1.56, 0.64, 1);
  backdrop-filter: blur(16px);
  box-shadow: 
    0 0 20px rgba(96, 165, 250, 0.05),
    0 20px 60px rgba(30, 58, 138, 0.15),
    inset 0 1px 1px rgba(255, 255, 255, 0.05);
  position: relative;
  overflow: hidden;
  z-index: 10;
  pointer-events: auto;
}

.about-bodhira-card::before {
  content: '';
  position: absolute;
  top: -40%;
  right: -40%;
  width: 280px;
  height: 280px;
  background: radial-gradient(circle, rgba(96, 165, 250, 0.15) 0%, transparent 70%);
  border-radius: 50%;
  filter: blur(50px);
  transition: all 0.7s ease;
  pointer-events: none;
  opacity: 0.3;
}

.about-bodhira-card:hover {
  transform: translateY(-16px);
  border-color: rgba(96, 165, 250, 0.5);
  background: linear-gradient(135deg, 
    rgba(30, 58, 138, 0.5) 0%, 
    rgba(59, 130, 246, 0.25) 50%,
    rgba(30, 58, 138, 0.5) 100%);
  box-shadow: 
    0 0 30px rgba(96, 165, 250, 0.08),
    0 30px 80px rgba(30, 58, 138, 0.25),
    inset 0 1px 1px rgba(255, 255, 255, 0.08);
}

.about-bodhira-card:hover::before {
  top: -20%;
  right: -20%;
  width: 350px;
  height: 350px;
  opacity: 0.4;
}

.card-header {
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  margin-bottom: 24px;
}

.card-icon {
  font-size: 56px;
  display: flex;
  align-items: center;
  justify-content: center;
  animation: subtleScale 2.5s ease-in-out infinite;
  background: linear-gradient(135deg, rgba(96, 165, 250, 0.15) 0%, rgba(147, 197, 253, 0.08) 100%);
  width: 80px;
  height: 80px;
  border-radius: 20px;
  border: 1px solid rgba(96, 165, 250, 0.2);
  transition: all 0.4s ease;
}

.card-badge {
  padding: 6px 14px;
  background: linear-gradient(135deg, rgba(96, 165, 250, 0.3) 0%, rgba(147, 197, 253, 0.2) 100%);
  border: 1px solid rgba(96, 165, 250, 0.4);
  border-radius: 20px;
  font-size: 11px;
  font-weight: 700;
  color: #93c5fd;
  letter-spacing: 0.4px;
  text-transform: uppercase;
}

.card-title {
  font-size: 26px;
  font-weight: 700;
  color: #f1f5f9;
  margin: 24px 0 16px 0;
  letter-spacing: -0.3px;
  line-height: 1.3;
}

.card-description {
  font-size: 15px;
  color: #cbd5e1;
  line-height: 1.8;
  margin: 0 0 auto 0;
  flex-grow: 1;
  font-weight: 400;
  letter-spacing: 0.2px;
}

.card-footer {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding-top: 24px;
  margin-top: 28px;
  cursor: pointer;
  pointer-events: auto;
  border-top: 1px solid rgba(96, 165, 250, 0.15);
  transition: all 0.3s ease;
}

.footer-text {
  font-size: 14px;
  color: #60a5fa;
  font-weight: 600;
  letter-spacing: 0.4px;
  cursor: pointer;
  pointer-events: auto;
  text-transform: uppercase;
  transition: all 0.3s ease;
}

.footer-arrow {
  font-size: 18px;
  font-weight: 700;
  color: #60a5fa;
  transition: transform 0.3s ease;
  cursor: pointer;
  pointer-events: auto;
}

.about-bodhira-card:hover .footer-text {
  color: #93c5fd;
}

.about-bodhira-card:hover .footer-arrow {
  transform: translateX(6px);
}

.about-bodhira-card:hover .card-icon {
  border-color: rgba(96, 165, 250, 0.4);
  background: linear-gradient(135deg, rgba(96, 165, 250, 0.25) 0%, rgba(147, 197, 253, 0.15) 100%);
}

/* ==================== ABOUT BODHIRA MODAL ==================== */

.modal-overlay {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: rgba(0, 0, 0, 0.7);
  backdrop-filter: blur(4px);
  z-index: 998;
  animation: fadeIn 0.3s ease-out;
}

.about-modal {
  position: fixed;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  max-width: 700px;
  width: 90%;
  max-height: 85vh;
  background: linear-gradient(135deg, #0f1429 0%, #141a2f 100%);
  border: 1px solid rgba(30, 58, 138, 0.3);
  border-radius: 24px;
  z-index: 999;
  display: flex;
  flex-direction: column;
  animation: slideInUp 0.4s cubic-bezier(0.34, 1.56, 0.64, 1);
  box-shadow: 0 20px 80px rgba(0, 0, 0, 0.5);
  overflow: hidden;
}

.modal-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 40px;
  border-bottom: 1px solid rgba(30, 58, 138, 0.2);
  background: linear-gradient(135deg, 
    rgba(30, 58, 138, 0.1) 0%, 
    rgba(59, 130, 246, 0.05) 100%);
}

.modal-header h2 {
  margin: 0;
  font-size: 32px;
  font-weight: 700;
  color: #f1f5f9;
  letter-spacing: -0.5px;
}

.modal-close-btn {
  background: rgba(30, 58, 138, 0.15);
  border: 1px solid rgba(30, 58, 138, 0.25);
  color: #cbd5e1;
  width: 40px;
  height: 40px;
  border-radius: 10px;
  font-size: 20px;
  font-weight: 600;
  cursor: pointer;
  transition: all 0.3s ease;
  display: flex;
  align-items: center;
  justify-content: center;
}

.modal-close-btn:hover {
  background: rgba(30, 58, 138, 0.25);
  border-color: rgba(30, 58, 138, 0.4);
  color: #f1f5f9;
  transform: rotate(90deg);
}

.modal-content {
  overflow-y: auto;
  padding: 40px;
  flex: 1;
}

.about-section {
  margin-bottom: 32px;
}

.about-section h3 {
  font-size: 20px;
  font-weight: 700;
  color: #60a5fa;
  margin: 0 0 12px 0;
  letter-spacing: -0.3px;
}

.about-section p {
  font-size: 15px;
  color: #cbd5e1;
  line-height: 1.7;
  margin: 0;
}

.about-list {
  list-style: none;
  padding: 0;
  margin: 0;
}

.about-list li {
  font-size: 15px;
  color: #cbd5e1;
  line-height: 1.8;
  margin-bottom: 10px;
  padding-left: 0;
}

.about-list li::before {
  content: '✓ ';
  color: #60a5fa;
  font-weight: 700;
  margin-right: 8px;
}

.modal-footer {
  display: flex;
  gap: 12px;
  padding: 24px 40px;
  border-top: 1px solid rgba(30, 58, 138, 0.2);
  background: linear-gradient(135deg, 
    rgba(30, 58, 138, 0.08) 0%, 
    rgba(59, 130, 246, 0.04) 100%);
  flex-wrap: wrap;
  justify-content: center;
}

.modal-footer .cta-button {
  flex: 1;
  min-width: 150px;
}

/* ==================== MODAL SCROLLBAR STYLING ==================== */

.modal-content::-webkit-scrollbar {
  width: 8px;
}

.modal-content::-webkit-scrollbar-track {
  background: transparent;
}

.modal-content::-webkit-scrollbar-thumb {
  background: rgba(30, 58, 138, 0.3);
  border-radius: 4px;
}

.modal-content::-webkit-scrollbar-thumb:hover {
  background: rgba(30, 58, 138, 0.5);
}

.hero-visual {
  position: relative;
  width: 100%;
  height: 100%;
  display: flex;
  align-items: center;
  justify-content: center;
}

.visual-shape {
  position: absolute;
  border-radius: 40% 60% 30% 70% / 60% 30% 70% 40%;
  opacity: 0.7;
  filter: blur(40px);
  animation: float 6s ease-in-out infinite;
}

.shape-1 {
  width: 280px;
  height: 280px;
  background: linear-gradient(135deg, #1e3a8a 0%, #1e40af 100%);
  top: 0;
  left: 0;
  animation: float 8s ease-in-out infinite;
}

.shape-2 {
  width: 200px;
  height: 200px;
  background: linear-gradient(135deg, #3b82f6 0%, #60a5fa 100%);
  bottom: 20px;
  right: 30px;
  animation: float 10s ease-in-out infinite reverse;
}

.shape-3 {
  width: 160px;
  height: 160px;
  background: linear-gradient(135deg, #0f172a 0%, #1e293b 100%);
  top: 50%;
  left: 30px;
  animation: float 12s ease-in-out infinite;
}

@keyframes float {
  0%, 100% { transform: translateY(0px); }
  50% { transform: translateY(-30px); }
}

@keyframes subtleScale {
  0%, 100% { transform: scale(1); }
  50% { transform: scale(1.05); }
}

.visual-glow {
  position: absolute;
  width: 400px;
  height: 400px;
  background: radial-gradient(circle, rgba(30, 58, 138, 0.15) 0%, transparent 70%);
  border-radius: 50%;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  filter: blur(60px);
}

/* Background Elements */
.hero-bg-elements {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  z-index: 0;
}

.bg-element {
  position: absolute;
  opacity: 0.03;
}

.element-1 {
  width: 400px;
  height: 400px;
  background: radial-gradient(circle, #60a5fa 0%, transparent 70%);
  top: -50px;
  right: -50px;
  border-radius: 50%;
}

.element-2 {
  width: 300px;
  height: 300px;
  background: radial-gradient(circle, #3b82f6 0%, transparent 70%);
  bottom: 100px;
  left: -100px;
  border-radius: 50%;
}

.element-3 {
  width: 200px;
  height: 200px;
  background: radial-gradient(circle, #1e40af 0%, transparent 70%);
  top: 50%;
  right: 10%;
  border-radius: 50%;
}

/* ==================== FEATURE HIGHLIGHTS - HORIZONTAL PILLS ==================== */

.features-highlights-section {
  position: relative;
  z-index: 2;
  padding: 40px 0;
  background: linear-gradient(180deg, transparent 0%, rgba(30, 58, 138, 0.05) 100%);
}

.features-container {
  max-width: 1200px;
  margin: 0 auto;
  padding: 0 40px;
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
  gap: 16px;
  animation: fadeIn 0.8s ease-out 0.5s both;
}

.feature-pill {
  display: flex;
  align-items: center;
  gap: 12px;
  padding: 14px 24px;
  background: rgba(30, 58, 138, 0.12);
  border: 1px solid rgba(30, 58, 138, 0.2);
  border-radius: 50px;
  transition: all 0.3s ease;
  cursor: pointer;
}

.feature-pill:hover {
  background: rgba(30, 58, 138, 0.18);
  border-color: rgba(30, 58, 138, 0.35);
  transform: translateY(-2px);
}

.pill-icon {
  font-size: 18px;
}

.pill-text {
  font-size: 14px;
  color: #e2e8f0;
  font-weight: 600;
  white-space: nowrap;
}

/* ==================== FEATURES SECTION ==================== */

.features-section {
  position: relative;
  z-index: 2;
  padding: 100px 40px;
  max-width: 1400px;
  margin: 0 auto;
}

.section-header {
  text-align: center;
  margin-bottom: 60px;
  animation: fadeInUp 0.8s ease-out;
}

.section-title {
  font-size: 44px;
  font-weight: 700;
  color: #f1f5f9;
  margin: 0 0 16px 0;
  letter-spacing: -0.5px;
}

.section-subtitle {
  font-size: 16px;
  color: #94a3b8;
  margin: 0;
  font-weight: 400;
}

.features-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
  gap: 24px;
}

.feature-card {
  padding: 40px;
  background: linear-gradient(135deg, rgba(15, 23, 42, 0.4) 0%, rgba(30, 41, 59, 0.3) 100%);
  border: 1px solid rgba(30, 58, 138, 0.15);
  border-radius: 16px;
  transition: all 0.3s cubic-bezier(0.34, 1.56, 0.64, 1);
  backdrop-filter: blur(8px);
  animation: fadeInUp 0.8s ease-out;
}

.feature-card:hover {
  background: linear-gradient(135deg, rgba(30, 58, 138, 0.15) 0%, rgba(30, 58, 138, 0.08) 100%);
  border-color: rgba(30, 58, 138, 0.3);
  transform: translateY(-8px);
  box-shadow: 0 12px 40px rgba(30, 58, 138, 0.15);
}

.feature-icon {
  font-size: 48px;
  margin-bottom: 16px;
  display: block;
}

.feature-card h3 {
  font-size: 20px;
  color: #f1f5f9;
  margin: 0 0 12px 0;
  font-weight: 600;
}

.feature-card p {
  font-size: 14px;
  color: #cbd5e1;
  margin: 0;
  line-height: 1.6;
}

/* ==================== STATS SECTION ==================== */

.stats-section {
  position: relative;
  z-index: 2;
  padding: 80px 40px;
  background: linear-gradient(180deg, rgba(30, 58, 138, 0.08) 0%, transparent 100%);
  max-width: 1200px;
  margin: 0 auto;
}

.stats-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
  gap: 32px;
  animation: fadeInUp 0.8s ease-out;
}

.stat-item {
  text-align: center;
}

.stat-number {
  font-size: 48px;
  font-weight: 700;
  background: linear-gradient(135deg, #60a5fa 0%, #93c5fd 100%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
  margin-bottom: 8px;
}

.stat-label {
  font-size: 14px;
  color: #94a3b8;
  font-weight: 600;
  letter-spacing: 0.3px;
}

/* ==================== FINAL CTA SECTION ==================== */

.final-cta-section {
  position: relative;
  z-index: 2;
  padding: 100px 40px;
  max-width: 1000px;
  margin: 0 auto;
  text-align: center;
}

.cta-content h2 {
  font-size: 48px;
  font-weight: 700;
  color: #f1f5f9;
  margin: 0 0 16px 0;
  letter-spacing: -0.5px;
  animation: fadeInUp 0.8s ease-out;
}

.cta-content p {
  font-size: 18px;
  color: #cbd5e1;
  margin: 0 0 48px 0;
  line-height: 1.6;
  animation: fadeInUp 0.8s ease-out 0.1s both;
}

.final-cta-buttons {
  display: flex;
  gap: 16px;
  justify-content: center;
  flex-wrap: wrap;
  animation: fadeInUp 0.8s ease-out 0.2s both;
}

/* ==================== ANIMATIONS ==================== */

@keyframes fadeInUp {
  from {
    opacity: 0;
    transform: translateY(30px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

@keyframes slideInUp {
  from {
    opacity: 0;
    transform: translateY(20px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

@keyframes slideInDown {
  from {
    opacity: 0;
    transform: translateY(-20px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

@keyframes fadeIn {
  from {
    opacity: 0;
  }
  to {
    opacity: 1;
  }
}

/* ==================== RESPONSIVE DESIGN ==================== */

@media (max-width: 1024px) {
  .hero-section {
    grid-template-columns: 1fr;
    gap: 60px;
    padding: 100px 48px;
    min-height: auto;
    justify-items: center;
  }

  .hero-left {
    max-width: 550px;
    width: 100%;
  }

  .hero-title {
    font-size: 48px;
  }

  .hero-subtitle {
    font-size: 16px;
  }

  .hero-right {
    height: auto;
    min-height: 420px;
    width: 100%;
  }

  .about-bodhira-card {
    max-width: 450px;
    height: auto;
    min-height: 360px;
  }

  .visual-shape {
    filter: blur(30px);
  }

  .section-title {
    font-size: 36px;
  }

  .cta-content h2 {
    font-size: 36px;
  }

  .about-modal {
    max-width: 600px;
    width: 95%;
  }
}

@media (max-width: 768px) {
  .hero-section {
    padding: 80px 28px;
    gap: 48px;
  }

  .hero-left {
    max-width: 100%;
    width: 100%;
  }

  .hero-title {
    font-size: 38px;
    margin-bottom: 20px;
  }

  .hero-subtitle {
    font-size: 15px;
    margin-bottom: 32px;
    max-width: 100%;
  }

  .hero-cta-buttons {
    flex-direction: column;
  }

  .cta-button {
    width: 100%;
    justify-content: center;
  }

  .hero-right {
    height: auto;
    min-height: 380px;
    width: 100%;
  }

  .about-bodhira-card {
    max-width: 100%;
    padding: 36px;
    min-height: 340px;
  }

  .card-title {
    font-size: 24px;
  }

  .card-description {
    font-size: 14px;
  }

  .features-container {
    grid-template-columns: repeat(2, 1fr);
    gap: 12px;
  }

  .feature-pill {
    padding: 12px 16px;
    font-size: 13px;
  }

  .features-section {
    padding: 60px 20px;
  }

  .section-header {
    margin-bottom: 40px;
  }

  .section-title {
    font-size: 28px;
  }

  .features-grid {
    grid-template-columns: 1fr;
    gap: 16px;
  }

  .feature-card {
    padding: 28px;
  }

  .feature-icon {
    font-size: 36px;
  }

  .feature-card h3 {
    font-size: 18px;
  }

  .feature-card p {
    font-size: 13px;
  }

  .stats-section {
    padding: 60px 20px;
  }

  .stats-grid {
    gap: 24px;
  }

  .stat-number {
    font-size: 36px;
  }

  .final-cta-section {
    padding: 60px 20px;
  }

  .cta-content h2 {
    font-size: 32px;
  }

  .cta-content p {
    font-size: 16px;
  }

  .final-cta-buttons {
    flex-direction: column;
  }

  .final-cta-buttons .cta-button {
    width: 100%;
  }

  .about-modal {
    max-width: 90%;
    width: 95%;
    max-height: 90vh;
  }

  .modal-header {
    padding: 30px;
  }

  .modal-header h2 {
    font-size: 26px;
  }

  .modal-close-btn {
    width: 36px;
    height: 36px;
    font-size: 18px;
  }

  .modal-content {
    padding: 30px;
  }

  .about-section h3 {
    font-size: 18px;
  }

  .about-section p,
  .about-list li {
    font-size: 14px;
  }

  .modal-footer {
    padding: 20px 30px;
    flex-direction: column;
  }

  .modal-footer .cta-button {
    width: 100%;
    min-width: unset;
  }
}

@media (max-width: 480px) {
  .hero-section {
    padding: 60px 18px;
    gap: 32px;
  }

  .hero-left {
    width: 100%;
  }

  .hero-title {
    font-size: 32px;
    margin-bottom: 18px;
  }

  .hero-subtitle {
    font-size: 14px;
    margin-bottom: 28px;
    max-width: 100%;
  }

  .hero-badge {
    margin-bottom: 20px;
  }

  .hero-right {
    height: auto;
    min-height: 320px;
    width: 100%;
  }

  .about-bodhira-card {
    max-width: 100%;
    padding: 28px;
    min-height: 300px;
  }

  .card-icon {
    font-size: 40px;
  }

  .card-title {
    font-size: 22px;
    margin-bottom: 10px;
  }

  .card-description {
    font-size: 13px;
    line-height: 1.5;
  }

  .card-footer {
    margin-top: 16px;
    padding-top: 16px;
  }

  .footer-text {
    font-size: 12px;
  }

  .features-container {
    grid-template-columns: 1fr;
    gap: 10px;
  }

  .feature-pill {
    font-size: 12px;
  }

  .features-section {
    padding: 40px 16px;
  }

  .section-title {
    font-size: 24px;
  }

  .feature-card {
    padding: 20px;
  }

  .feature-icon {
    font-size: 32px;
    margin-bottom: 12px;
  }

  .feature-card h3 {
    font-size: 16px;
  }

  .feature-card p {
    font-size: 12px;
  }

  .stats-section {
    padding: 40px 16px;
  }

  .stats-grid {
    gap: 16px;
  }

  .stat-number {
    font-size: 28px;
  }

  .final-cta-section {
    padding: 40px 16px;
  }

  .cta-content h2 {
    font-size: 24px;
  }

  .cta-content p {
    font-size: 14px;
    margin-bottom: 32px;
  }

  .about-modal {
    max-width: 95%;
    width: 100%;
    max-height: 95vh;
  }

  .modal-header {
    padding: 24px;
    flex-direction: column;
    gap: 12px;
    align-items: flex-start;
  }

  .modal-header h2 {
    font-size: 22px;
  }

  .modal-close-btn {
    align-self: flex-end;
    width: 32px;
    height: 32px;
    font-size: 16px;
  }

  .modal-content {
    padding: 24px;
  }

  .about-section {
    margin-bottom: 24px;
  }

  .about-section h3 {
    font-size: 16px;
    margin-bottom: 8px;
  }

  .about-section p,
  .about-list li {
    font-size: 13px;
    line-height: 1.6;
  }

  .about-list li {
    margin-bottom: 8px;
  }

  .modal-footer {
    padding: 16px 24px;
    gap: 8px;
  }

  .modal-footer .cta-button {
    padding: 12px 16px;
    font-size: 14px;
  }
}

/* ==================== LIGHT THEME - Home Page ==================== */

:root[data-theme='light'] .hero-container {
  background: linear-gradient(180deg, #f8fafc 0%, #f1f5f9 50%, #f8fafc 100%);
}

:root[data-theme='light'] .hero-bg-shape {
  opacity: 0.02;
}

:root[data-theme='light'] .hero-title {
  color: #0f1728;
}

:root[data-theme='light'] .hero-subtitle {
  color: #475569;
}

:root[data-theme='light'] .hero-cta {
  background: linear-gradient(135deg, #2563eb 0%, #1d4ed8 100%);
  box-shadow: 0 8px 24px rgba(37, 99, 235, 0.2);
}

:root[data-theme='light'] .hero-cta:hover {
  box-shadow: 0 12px 32px rgba(37, 99, 235, 0.3);
}

:root[data-theme='light'] .hero-secondary-cta {
  color: #2563eb;
  border-color: rgba(37, 99, 235, 0.3);
}

:root[data-theme='light'] .hero-secondary-cta:hover {
  background: rgba(37, 99, 235, 0.08);
  border-color: rgba(37, 99, 235, 0.5);
}

:root[data-theme='light'] .feature-card {
  background: linear-gradient(135deg, rgba(255, 255, 255, 0.8) 0%, rgba(248, 250, 252, 0.8) 100%);
  border-color: rgba(226, 232, 240, 0.6);
  box-shadow: 0 4px 12px rgba(100, 116, 139, 0.08);
}

:root[data-theme='light'] .feature-card:hover {
  background: linear-gradient(135deg, rgba(255, 255, 255, 0.95) 0%, rgba(248, 250, 252, 0.95) 100%);
  border-color: rgba(203, 213, 225, 0.8);
  box-shadow: 0 8px 20px rgba(100, 116, 139, 0.12);
}

:root[data-theme='light'] .feature-card-title {
  color: #0f1728;
}

:root[data-theme='light'] .feature-card-desc {
  color: #475569;
}

:root[data-theme='light'] .pricing-card {
  background: linear-gradient(135deg, rgba(255, 255, 255, 0.9) 0%, rgba(248, 250, 252, 0.9) 100%);
  border-color: rgba(226, 232, 240, 0.8);
  box-shadow: 0 8px 24px rgba(100, 116, 139, 0.1);
}

:root[data-theme='light'] .pricing-card:hover {
  background: linear-gradient(135deg, rgba(255, 255, 255, 0.98) 0%, rgba(248, 250, 252, 0.98) 100%);
  border-color: rgba(203, 213, 225, 0.9);
  box-shadow: 0 12px 32px rgba(100, 116, 139, 0.15);
}

:root[data-theme='light'] .pricing-card-title {
  color: #0f1728;
}

:root[data-theme='light'] .pricing-card-price {
  color: #0f1728;
}

:root[data-theme='light'] .pricing-card-desc {
  color: #475569;
}

:root[data-theme='light'] .pricing-feature {
  color: #64748b;
}

:root[data-theme='light'] .cta-button {
  background: linear-gradient(135deg, #2563eb 0%, #1d4ed8 100%);
  box-shadow: 0 8px 24px rgba(37, 99, 235, 0.2);
}

:root[data-theme='light'] .cta-button:hover {
  background: linear-gradient(135deg, #1d4ed8 0%, #1e40af 100%);
  box-shadow: 0 12px 32px rgba(37, 99, 235, 0.3);
}

:root[data-theme='light'] .navbar {
  background: linear-gradient(135deg, rgba(248, 250, 252, 0.98) 0%, rgba(241, 245, 249, 0.98) 100%);
  border-bottom-color: rgba(226, 232, 240, 0.6);
  box-shadow: 0 4px 20px rgba(100, 116, 139, 0.1);
}

:root[data-theme='light'] .navbar a,
:root[data-theme='light'] .navbar button {
  color: #0f1728;
}

:root[data-theme='light'] .navbar a:hover {
  color: #2563eb;
}

:root[data-theme='light'] .about-header h2 {
  color: #0f1728;
}

:root[data-theme='light'] .about-intro {
  color: #475569;
}

:root[data-theme='light'] .about-description {
  color: #64748b;
}

:root[data-theme='light'] .stats-grid-item {
  background: linear-gradient(135deg, rgba(255, 255, 255, 0.8) 0%, rgba(248, 250, 252, 0.8) 100%);
  border-color: rgba(226, 232, 240, 0.6);
}

:root[data-theme='light'] .stats-grid-item-value {
  color: #0f1728;
}

:root[data-theme='light'] .stats-grid-item-label {
  color: #475569;
}

/* ==================== ABOUT SECTION ==================== */

.about-section {
  padding: 100px 40px;
  max-width: 1400px;
  margin: 0 auto;
  position: relative;
  z-index: 2;
}

.about-container {
  display: flex;
  flex-direction: column;
  gap: 60px;
}

.about-header {
  text-align: center;
  margin-bottom: 40px;
  display: flex;
  flex-direction: column;
  align-items: center;
  width: 100%;
}

.about-header h2 {
  font-size: 48px;
  font-weight: 700;
  color: #e2e8f0;
  margin: 0 0 24px 0;
  letter-spacing: -1px;
  line-height: 1.3;
  text-align: center;
  max-width: 100%;
}

.about-intro {
  font-size: 17px;
  color: #a0aec0;
  margin: 0 auto 24px auto;
  line-height: 2;
  max-width: 900px;
  text-align: center;
  display: block;
  width: 100%;
  letter-spacing: 0.3px;
  font-weight: 400;
}

.about-intro-bold {
  font-size: 24px;
  color: #3b82f6;
  margin: 24px auto 0 auto;
  font-weight: 700;
  text-align: center;
  max-width: 900px;
  display: block;
  width: 100%;
  letter-spacing: 0.5px;
  background: linear-gradient(135deg, #60a5fa 0%, #3b82f6 100%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
}

.about-description {
  text-align: center;
  margin: 0;
  padding: 40px;
  background: linear-gradient(135deg, rgba(59, 130, 246, 0.1) 0%, rgba(139, 92, 246, 0.1) 100%);
  border: 1px solid rgba(148, 163, 184, 0.2);
  border-radius: 16px;
  backdrop-filter: blur(10px);
}

.about-description p {
  font-size: 16px;
  color: #cbd5e1;
  margin: 0;
  line-height: 1.8;
  max-width: 900px;
  margin-left: auto;
  margin-right: auto;
}

.about-section-divider {
  text-align: center;
  padding: 40px 0 20px 0;
  border-top: 1px solid rgba(148, 163, 184, 0.2);
  margin: 40px 0 0 0;
}

.about-section-divider h3 {
  font-size: 36px;
  font-weight: 700;
  color: #e2e8f0;
  margin: 0;
  letter-spacing: -0.5px;
}

.about-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
  gap: 30px;
  margin: 40px 0;
}

.about-card {
  background: linear-gradient(135deg, rgba(59, 130, 246, 0.1) 0%, rgba(139, 92, 246, 0.1) 100%);
  border: 1px solid rgba(148, 163, 184, 0.2);
  border-radius: 16px;
  padding: 40px;
  backdrop-filter: blur(10px);
  transition: all 0.3s ease;
  box-shadow: 0 8px 32px rgba(30, 58, 138, 0.15);
}

.about-card:hover {
  transform: translateY(-8px);
  border-color: rgba(148, 163, 184, 0.4);
  box-shadow: 0 16px 48px rgba(30, 58, 138, 0.25);
  background: linear-gradient(135deg, rgba(59, 130, 246, 0.15) 0%, rgba(139, 92, 246, 0.15) 100%);
}

.about-card h4 {
  font-size: 18px;
  color: #e2e8f0;
  margin: 0 0 16px 0;
  font-weight: 700;
}

.about-card p {
  font-size: 15px;
  color: #cbd5e1;
  line-height: 1.8;
  margin: 0;
}

.about-final-message {
  text-align: center;
  padding: 60px 40px;
  background: linear-gradient(135deg, rgba(59, 130, 246, 0.1) 0%, rgba(139, 92, 246, 0.1) 100%);
  border: 1px solid rgba(148, 163, 184, 0.2);
  border-radius: 16px;
  backdrop-filter: blur(10px);
  margin: 40px 0;
}

.about-final-message p {
  font-size: 16px;
  color: #cbd5e1;
  line-height: 1.9;
  margin: 0 0 24px 0;
  max-width: 900px;
  margin-left: auto;
  margin-right: auto;
}

.about-final-message p:last-child {
  margin-bottom: 0;
}

.about-tagline {
  font-size: 18px;
  color: #60a5fa;
  margin: 24px 0 0 0;
}

.about-tagline strong {
  color: #e2e8f0;
  font-weight: 700;
}

.about-cta {
  text-align: center;
  padding: 40px 20px;
  border-top: 1px solid rgba(148, 163, 184, 0.2);
  margin-top: 40px;
}

.about-cta .cta-button {
  display: inline-flex;
  align-items: center;
  gap: 8px;
}

/* Light mode for about section */
:root[data-theme='light'] .about-header h2 {
  color: #1e293b;
}

:root[data-theme='light'] .about-intro {
  color: #475569;
}

:root[data-theme='light'] .about-intro-bold {
  color: #2563eb;
}

:root[data-theme='light'] .about-description {
  background: linear-gradient(135deg, rgba(59, 130, 246, 0.05) 0%, rgba(139, 92, 246, 0.05) 100%);
  border-color: rgba(226, 232, 240, 0.8);
}

:root[data-theme='light'] .about-description p {
  color: #475569;
}

:root[data-theme='light'] .about-section-divider {
  border-top-color: rgba(226, 232, 240, 0.6);
}

:root[data-theme='light'] .about-section-divider h3 {
  color: #1e293b;
}

:root[data-theme='light'] .about-card {
  background: linear-gradient(135deg, rgba(59, 130, 246, 0.05) 0%, rgba(139, 92, 246, 0.05) 100%);
  border-color: rgba(226, 232, 240, 0.8);
  box-shadow: 0 8px 32px rgba(100, 116, 139, 0.1);
}

:root[data-theme='light'] .about-card:hover {
  border-color: rgba(226, 232, 240, 1);
  box-shadow: 0 16px 48px rgba(100, 116, 139, 0.2);
  background: linear-gradient(135deg, rgba(59, 130, 246, 0.1) 0%, rgba(139, 92, 246, 0.1) 100%);
}

:root[data-theme='light'] .about-card h4 {
  color: #1e293b;
}

:root[data-theme='light'] .about-card p {
  color: #475569;
}

:root[data-theme='light'] .about-final-message {
  background: linear-gradient(135deg, rgba(59, 130, 246, 0.05) 0%, rgba(139, 92, 246, 0.05) 100%);
  border-color: rgba(226, 232, 240, 0.8);
}

:root[data-theme='light'] .about-final-message p {
  color: #475569;
}

:root[data-theme='light'] .about-tagline {
  color: #2563eb;
}

:root[data-theme='light'] .about-tagline strong {
  color: #1e293b;
}

:root[data-theme='light'] .about-cta {
  border-top-color: rgba(226, 232, 240, 0.6);
}
