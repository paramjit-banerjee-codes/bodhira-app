/* ==================== DASHBOARD - PREMIUM DESIGN ==================== */

.dashboard-container {
  min-height: 100vh;
  background: linear-gradient(180deg, 
    #0a0e27 0%, 
    #0f1429 25%, 
    #141a2f 50%, 
    #0f1429 75%, 
    #0a0e27 100%);
  padding: 48px 20px 80px;
  font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
  position: relative;
}

/* ==================== HEADER ==================== */

.dashboard-header {
  max-width: 1200px;
  margin: 0 auto 56px;
  padding-bottom: 40px;
  border-bottom: 1.5px solid rgba(96, 165, 250, 0.15);
  position: relative;
}

.dashboard-header::after {
  content: '';
  position: absolute;
  bottom: -1.5px;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, transparent, rgba(96, 165, 250, 0.4), transparent);
}

.dashboard-header h1 {
  font-size: 44px;
  font-weight: 800;
  letter-spacing: -1px;
  color: #f1f5f9;
  margin: 0 0 12px 0;
  line-height: 1.2;
  background: linear-gradient(135deg, #f1f5f9 0%, #cbd5e1 100%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
}

.header-subtitle {
  font-size: 16px;
  color: #94a3b8;
  font-weight: 400;
  margin: 0;
  letter-spacing: 0.2px;
}

/* ==================== QUICK STATS ==================== */

.quick-stats {
  max-width: 1200px;
  margin: 0 auto 40px;
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
  gap: 24px;
}

.stat-item {
  display: flex;
  flex-direction: column;
  gap: 12px;
  padding: 28px;
  background: linear-gradient(135deg, 
    rgba(30, 58, 138, 0.15) 0%, 
    rgba(59, 130, 246, 0.1) 50%,
    rgba(30, 58, 138, 0.1) 100%);
  border: 1.5px solid rgba(96, 165, 250, 0.2);
  border-radius: 16px;
  backdrop-filter: blur(15px);
  transition: all 0.4s cubic-bezier(0.34, 1.56, 0.64, 1);
  position: relative;
  overflow: hidden;
  box-shadow: 0 8px 24px rgba(0, 0, 0, 0.15);
}

.stat-item::before {
  content: '';
  position: absolute;
  top: -50%;
  right: -50%;
  width: 150px;
  height: 150px;
  background: radial-gradient(circle, rgba(96, 165, 250, 0.25) 0%, transparent 70%);
  border-radius: 50%;
  filter: blur(40px);
  transition: all 0.6s ease;
}

.stat-item::after {
  content: '';
  position: absolute;
  bottom: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, transparent, rgba(96, 165, 250, 0.3), transparent);
}

.stat-item:hover {
  transform: translateY(-8px);
  background: linear-gradient(135deg, 
    rgba(30, 58, 138, 0.25) 0%, 
    rgba(59, 130, 246, 0.15) 50%,
    rgba(30, 58, 138, 0.2) 100%);
  border-color: rgba(96, 165, 250, 0.5);
  box-shadow: 0 16px 48px rgba(59, 130, 246, 0.25);
}

.stat-item:hover::before {
  top: -30%;
  right: -30%;
  width: 200px;
  height: 200px;
}

.stat-icon {
  font-size: 32px;
  display: block;
  opacity: 0.9;
}

.stat-label {
  font-size: 12px;
  color: #94a3b8;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 0.7px;
}

.stat-value {
  font-size: 44px;
  font-weight: 900;
  background: linear-gradient(135deg, #60a5fa 0%, #93c5fd 100%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
  letter-spacing: -1px;
  line-height: 1;
}

.stat-trend {
  display: flex;
  gap: 8px;
  align-items: center;
  margin-top: 4px;
}

.trend-indicator {
  font-size: 11px;
  color: #64748b;
  font-weight: 600;
  padding: 4px 10px;
  background: rgba(96, 165, 250, 0.08);
  border-radius: 6px;
  letter-spacing: 0.3px;
}

/* ==================== CTA BUTTON ==================== */

.dashboard-cta {
  max-width: 1200px;
  margin: 0 auto 48px;
}

.cta-button {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: 12px;
  padding: 16px 36px;
  background: linear-gradient(135deg, #1e3a8a 0%, #1e40af 100%);
  color: white;
  border: 1px solid rgba(30, 58, 138, 0.5);
  border-radius: 12px;
  font-size: 15px;
  font-weight: 600;
  text-decoration: none;
  cursor: pointer;
  transition: all 0.3s cubic-bezier(0.34, 1.56, 0.64, 1);
  box-shadow: 0 12px 32px rgba(30, 58, 138, 0.25);
  position: relative;
  overflow: hidden;
}

.cta-button::before {
  content: '';
  position: absolute;
  top: 0;
  left: -100%;
  width: 100%;
  height: 100%;
  background: rgba(255, 255, 255, 0.1);
  transition: left 0.3s ease;
}

.cta-button:hover {
  transform: translateY(-4px);
  box-shadow: 0 16px 48px rgba(30, 58, 138, 0.35);
  background: linear-gradient(135deg, #1e40af 0%, #3b82f6 100%);
}

.cta-button:hover::before {
  left: 100%;
}

.cta-button:active {
  transform: translateY(-2px);
}

.cta-icon {
  font-size: 18px;
  font-weight: 700;
}

.cta-text {
  font-weight: 600;
}

/* ==================== EMPTY STATE ==================== */

.empty-state {
  max-width: 1200px;
  margin: 0 auto;
  padding: 100px 40px;
  text-align: center;
  background: linear-gradient(135deg, 
    rgba(30, 58, 138, 0.1) 0%, 
    rgba(59, 130, 246, 0.05) 50%,
    rgba(30, 58, 138, 0.08) 100%);
  border: 1px solid rgba(30, 58, 138, 0.2);
  border-radius: 20px;
  backdrop-filter: blur(10px);
  position: relative;
  overflow: hidden;
}

.empty-state::before {
  content: '';
  position: absolute;
  top: -50%;
  right: -50%;
  width: 300px;
  height: 300px;
  background: radial-gradient(circle, rgba(96, 165, 250, 0.15) 0%, transparent 70%);
  border-radius: 50%;
  filter: blur(60px);
}

.empty-state::after {
  content: '';
  position: absolute;
  bottom: -50%;
  left: -50%;
  width: 250px;
  height: 250px;
  background: radial-gradient(circle, rgba(59, 130, 246, 0.1) 0%, transparent 70%);
  border-radius: 50%;
  filter: blur(60px);
}

.empty-icon {
  width: 100px;
  height: 100px;
  margin: 0 auto 28px;
  background: url('/logo2.png') center / contain no-repeat;
  background-color: rgba(59, 130, 246, 0.1);
  border-radius: 20px;
  display: flex;
  align-items: center;
  justify-content: center;
  position: relative;
  z-index: 1;
  border: 2px solid rgba(59, 130, 246, 0.2);
  box-shadow: 0 8px 32px rgba(59, 130, 246, 0.15);
}

.empty-icon-img {
  width: 80px;
  height: 80px;
  object-fit: contain;
  filter: drop-shadow(0 0 20px rgba(96, 165, 250, 0.3));
}

.empty-state h2 {
  font-size: 28px;
  color: #f1f5f9;
  font-weight: 700;
  margin: 0 0 16px 0;
  letter-spacing: -0.5px;
  position: relative;
  z-index: 1;
}

.empty-state p {
  font-size: 16px;
  color: #cbd5e1;
  margin: 0 0 40px 0;
  position: relative;
  z-index: 1;
  line-height: 1.6;
}

.empty-cta {
  display: inline-flex;
  align-items: center;
  gap: 10px;
  padding: 14px 32px;
  background: linear-gradient(135deg, #1e3a8a 0%, #1e40af 100%);
  color: white;
  border: 1px solid rgba(30, 58, 138, 0.5);
  border-radius: 12px;
  font-size: 15px;
  font-weight: 700;
  text-decoration: none;
  cursor: pointer;
  transition: all 0.3s cubic-bezier(0.34, 1.56, 0.64, 1);
  position: relative;
  z-index: 1;
  box-shadow: 0 12px 32px rgba(30, 58, 138, 0.25);
}

.empty-cta:hover {
  transform: translateY(-4px);
  box-shadow: 0 16px 48px rgba(30, 58, 138, 0.35);
  background: linear-gradient(135deg, #1e40af 0%, #3b82f6 100%);
}

/* ==================== TESTS CONTAINER ==================== */

.tests-container {
  max-width: 1200px;
  margin: 0 auto;
}

.tests-list {
  display: flex;
  flex-direction: column;
  gap: 12px;
}

/* ==================== TEST ITEM ==================== */

.test-item {
  background: linear-gradient(135deg, 
    rgba(15, 23, 42, 0.5) 0%, 
    rgba(30, 41, 59, 0.3) 100%);
  border: 1px solid rgba(30, 58, 138, 0.15);
  border-radius: 16px;
  padding: 32px;
  transition: all 0.4s cubic-bezier(0.34, 1.56, 0.64, 1);
  backdrop-filter: blur(10px);
  position: relative;
  overflow: hidden;
}

.test-item::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, transparent, rgba(96, 165, 250, 0.3), transparent);
}

.test-item:hover {
  background: linear-gradient(135deg, 
    rgba(15, 23, 42, 0.7) 0%, 
    rgba(30, 41, 59, 0.45) 100%);
  border-color: rgba(96, 165, 250, 0.3);
  transform: translateY(-6px);
  box-shadow: 0 20px 60px rgba(30, 58, 138, 0.2);
}

.test-item-header {
  display: flex;
  justify-content: space-between;
  align-items: flex-start;
  gap: 20px;
  margin-bottom: 24px;
}

.test-item-title-section {
  flex: 1;
}

.test-item-title {
  font-size: 20px;
  font-weight: 700;
  color: #f1f5f9;
  margin: 0 0 8px 0;
  line-height: 1.4;
  letter-spacing: -0.3px;
}

.test-item-topic {
  font-size: 14px;
  color: #94a3b8;
  margin: 0;
  font-weight: 500;
}

.test-item-badge {
  flex-shrink: 0;
}

.badge {
  display: inline-flex;
  padding: 8px 16px;
  border-radius: 8px;
  font-size: 12px;
  font-weight: 700;
  letter-spacing: 0.4px;
  text-transform: uppercase;
}

.badge-published {
  background: linear-gradient(135deg, rgba(34, 197, 94, 0.15) 0%, rgba(34, 197, 94, 0.08) 100%);
  color: #86efac;
  border: 1px solid rgba(34, 197, 94, 0.3);
}

.badge-draft {
  background: linear-gradient(135deg, rgba(107, 114, 128, 0.15) 0%, rgba(107, 114, 128, 0.08) 100%);
  color: #d1d5db;
  border: 1px solid rgba(107, 114, 128, 0.3);
}

/* ==================== TEST META ==================== */

.test-item-meta {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(160px, 1fr));
  gap: 24px;
  margin-bottom: 28px;
  padding-bottom: 28px;
  border-bottom: 1px solid rgba(30, 58, 138, 0.15);
}

.meta-group {
  display: flex;
  flex-direction: column;
  gap: 8px;
}

.meta-label {
  font-size: 12px;
  color: #94a3b8;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 0.5px;
}

.meta-value {
  font-size: 16px;
  color: #e2e8f0;
  font-weight: 600;
}

.code-value {
  font-family: 'SF Mono', 'Monaco', 'Menlo', monospace;
  font-size: 17px;
  color: #60a5fa;
  font-weight: 700;
  letter-spacing: 0.8px;
}

/* ==================== TEST ACTIONS ==================== */

.test-item-actions {
  display: flex;
  gap: 12px;
  flex-wrap: wrap;
}

.action-button {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  padding: 11px 20px;
  border-radius: 10px;
  font-size: 13px;
  font-weight: 700;
  border: none;
  cursor: pointer;
  transition: all 0.3s cubic-bezier(0.34, 1.56, 0.64, 1);
  text-decoration: none;
  white-space: nowrap;
  letter-spacing: 0.2px;
  text-transform: uppercase;
}

.action-primary {
  background: linear-gradient(135deg, rgba(30, 58, 138, 0.25) 0%, rgba(30, 58, 138, 0.15) 100%);
  color: #60a5fa;
  border: 1px solid rgba(30, 58, 138, 0.4);
}

.action-primary:hover {
  background: linear-gradient(135deg, rgba(30, 58, 138, 0.35) 0%, rgba(30, 58, 138, 0.25) 100%);
  border-color: rgba(96, 165, 250, 0.5);
  color: #93c5fd;
  transform: translateY(-2px);
  box-shadow: 0 8px 20px rgba(30, 58, 138, 0.15);
}

.action-secondary {
  background: linear-gradient(135deg, rgba(255, 255, 255, 0.06) 0%, rgba(255, 255, 255, 0.03) 100%);
  color: #cbd5e1;
  border: 1px solid rgba(255, 255, 255, 0.1);
}

.action-secondary:hover {
  background: linear-gradient(135deg, rgba(255, 255, 255, 0.1) 0%, rgba(255, 255, 255, 0.06) 100%);
  border-color: rgba(255, 255, 255, 0.15);
  color: #f1f5f9;
  transform: translateY(-2px);
  box-shadow: 0 8px 20px rgba(255, 255, 255, 0.05);
}

.action-danger {
  background: linear-gradient(135deg, rgba(239, 68, 68, 0.15) 0%, rgba(239, 68, 68, 0.08) 100%);
  color: #fca5a5;
  border: 1px solid rgba(239, 68, 68, 0.3);
}

.action-danger:hover {
  background: linear-gradient(135deg, rgba(239, 68, 68, 0.25) 0%, rgba(239, 68, 68, 0.15) 100%);
  border-color: rgba(239, 68, 68, 0.4);
  color: #fecaca;
  transform: translateY(-2px);
  box-shadow: 0 8px 20px rgba(239, 68, 68, 0.1);
}

/* ==================== LOADING SPINNER ==================== */

.loading-spinner {
  max-width: 1200px;
  margin: 100px auto;
  width: 56px;
  height: 56px;
  border: 3px solid rgba(96, 165, 250, 0.2);
  border-top-color: #60a5fa;
  border-radius: 50%;
  animation: spin 0.8s linear infinite;
  box-shadow: 0 0 30px rgba(96, 165, 250, 0.2);
}

@keyframes spin {
  to {
    transform: rotate(360deg);
  }
}

@keyframes fadeIn {
  from {
    opacity: 0;
    transform: scale(0.95);
  }
  to {
    opacity: 1;
    transform: scale(1);
  }
}

@keyframes float {
  0%, 100% {
    transform: translateY(0px);
  }
  50% {
    transform: translateY(-12px);
  }
}

/* ==================== TIPS SECTION ==================== */

.dashboard-tips-section {
  max-width: 1200px;
  margin: 80px auto 0;
  padding-top: 48px;
  border-top: 1px solid rgba(255, 255, 255, 0.03);
}

/* ==================== RESPONSIVE ==================== */

@media (max-width: 768px) {
  .dashboard-container {
    padding: 32px 16px 60px;
  }

  .dashboard-header {
    margin-bottom: 40px;
    padding-bottom: 28px;
  }

  .dashboard-header h1 {
    font-size: 32px;
    margin-bottom: 8px;
  }

  .header-subtitle {
    font-size: 15px;
  }

  .quick-stats {
    gap: 16px;
    margin-bottom: 32px;
    grid-template-columns: repeat(auto-fit, minmax(160px, 1fr));
  }

  .stat-item {
    padding: 20px;
  }

  .stat-value {
    font-size: 32px;
  }

  .cta-button {
    padding: 14px 28px;
    font-size: 14px;
  }

  .test-item {
    padding: 20px;
  }

  .test-item-header {
    margin-bottom: 18px;
    flex-direction: column;
    gap: 12px;
  }

  .test-item-title {
    font-size: 18px;
  }

  .test-item-topic {
    font-size: 13px;
  }

  .test-item-meta {
    gap: 16px;
    margin-bottom: 18px;
    padding-bottom: 18px;
    grid-template-columns: repeat(2, 1fr);
  }

  .test-item-actions {
    gap: 8px;
  }

  .action-button {
    padding: 9px 14px;
    font-size: 12px;
    flex: 1 0 calc(50% - 4px);
  }

  .empty-state {
    padding: 60px 24px;
  }

  .empty-icon {
    font-size: 48px;
  }

  .empty-state h2 {
    font-size: 24px;
  }

  .empty-state p {
    font-size: 14px;
    margin-bottom: 28px;
  }

  .empty-cta {
    padding: 12px 24px;
    font-size: 14px;
  }

  .dashboard-tips-section {
    margin-top: 60px;
    padding-top: 32px;
  }
}

@media (max-width: 480px) {
  .dashboard-container {
    padding: 24px 12px 40px;
  }

  .dashboard-header {
    margin-bottom: 28px;
    padding-bottom: 20px;
  }

  .dashboard-header h1 {
    font-size: 26px;
  }

  .header-subtitle {
    font-size: 14px;
  }

  .quick-stats {
    grid-template-columns: 1fr;
    gap: 14px;
    margin-bottom: 24px;
  }

  .stat-item {
    padding: 18px;
    flex-direction: row;
    align-items: center;
    gap: 16px;
  }

  .stat-icon {
    font-size: 28px;
    flex-shrink: 0;
  }

  .stat-value {
    font-size: 26px;
  }

  .stat-label {
    font-size: 11px;
  }

  .trend-indicator {
    font-size: 10px;
  }

  .cta-button {
    width: 100%;
    padding: 13px 20px;
  }

  .test-item {
    padding: 16px;
  }

  .test-item-meta {
    grid-template-columns: 1fr;
    gap: 12px;
    margin-bottom: 14px;
    padding-bottom: 14px;
  }

  .action-button {
    flex: 1 0 100%;
    min-width: auto;
    padding: 10px 12px;
  }

  .empty-state {
    padding: 40px 16px;
  }

  .empty-icon {
    font-size: 40px;
    margin-bottom: 16px;
  }

  .empty-state h2 {
    font-size: 20px;
  }

  .empty-state p {
    font-size: 13px;
    margin-bottom: 20px;
  }

  .empty-cta {
    width: 100%;
    padding: 11px 20px;
    font-size: 13px;
  }
}

/* ==================== SKELETON LOADER - PREMIUM DESIGN ==================== */

@keyframes shimmer {
  0% {
    background-color: rgba(96, 165, 250, 0.08);
    background-image: linear-gradient(
      90deg,
      rgba(96, 165, 250, 0.08) 0%,
      rgba(96, 165, 250, 0.15) 50%,
      rgba(96, 165, 250, 0.08) 100%
    );
    background-size: 200% 100%;
    background-position: 200% 0;
  }
  100% {
    background-color: rgba(96, 165, 250, 0.15);
    background-image: linear-gradient(
      90deg,
      rgba(96, 165, 250, 0.08) 0%,
      rgba(96, 165, 250, 0.15) 50%,
      rgba(96, 165, 250, 0.08) 100%
    );
    background-size: 200% 100%;
    background-position: -200% 0;
  }
}

@keyframes fadeIn {
  from {
    opacity: 0;
  }
  to {
    opacity: 1;
  }
}

@keyframes fadeOut {
  from {
    opacity: 1;
  }
  to {
    opacity: 0;
  }
}

.skeleton-loader {
  max-width: 1200px;
  margin: 0 auto;
  padding: 0 20px;
  animation: fadeIn 0.4s ease-out;
}

.skeleton-block {
  background-color: rgba(96, 165, 250, 0.08);
  border-radius: 12px;
  animation: shimmer 2.5s ease-in-out infinite;
}

/* Header Skeleton */
.skeleton-header {
  margin-bottom: 56px;
  padding-bottom: 40px;
  border-bottom: 1.5px solid rgba(96, 165, 250, 0.1);
  animation: fadeIn 0.4s ease-out 0.05s both;
}

.skeleton-title {
  height: 48px;
  background: linear-gradient(
    90deg,
    rgba(96, 165, 250, 0.1) 0%,
    rgba(96, 165, 250, 0.18) 50%,
    rgba(96, 165, 250, 0.1) 100%
  );
  border-radius: 14px;
  margin-bottom: 20px;
  width: 50%;
  animation: shimmer 2.5s ease-in-out infinite;
  background-size: 200% 100%;
  background-position: 200% 0;
}

.skeleton-subtitle {
  height: 18px;
  background: linear-gradient(
    90deg,
    rgba(96, 165, 250, 0.08) 0%,
    rgba(96, 165, 250, 0.14) 50%,
    rgba(96, 165, 250, 0.08) 100%
  );
  border-radius: 9px;
  width: 65%;
  animation: shimmer 2.5s ease-in-out infinite;
  background-size: 200% 100%;
  background-position: 200% 0;
}

/* Stats Skeleton */
.skeleton-stats {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
  gap: 24px;
  margin-bottom: 56px;
  animation: fadeIn 0.4s ease-out 0.1s both;
}

.skeleton-stat-card {
  background: rgba(30, 41, 59, 0.25);
  border: 1px solid rgba(96, 165, 250, 0.12);
  border-radius: 16px;
  padding: 32px;
  height: 160px;
  animation: shimmer 2.5s ease-in-out infinite;
  position: relative;
  overflow: hidden;
}

.skeleton-stat-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: linear-gradient(
    90deg,
    rgba(96, 165, 250, 0.08) 0%,
    rgba(96, 165, 250, 0.16) 50%,
    rgba(96, 165, 250, 0.08) 100%
  );
  animation: shimmer 2.5s ease-in-out infinite;
  background-size: 200% 100%;
  background-position: 200% 0;
}

/* CTA Skeleton */
.skeleton-cta {
  height: 56px;
  background: linear-gradient(
    90deg,
    rgba(96, 165, 250, 0.1) 0%,
    rgba(96, 165, 250, 0.18) 50%,
    rgba(96, 165, 250, 0.1) 100%
  );
  border-radius: 12px;
  margin-bottom: 56px;
  width: 240px;
  animation: shimmer 2.5s ease-in-out infinite;
  background-size: 200% 100%;
  background-position: 200% 0;
}

/* Test Items Skeleton */
.skeleton-tests {
  display: flex;
  flex-direction: column;
  gap: 24px;
  animation: fadeIn 0.4s ease-out 0.15s both;
}

.skeleton-test-item {
  background: rgba(30, 41, 59, 0.25);
  border: 1px solid rgba(96, 165, 250, 0.12);
  border-radius: 16px;
  padding: 28px;
  animation: fadeIn 0.4s ease-out both;
  position: relative;
  overflow: hidden;
}

.skeleton-test-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  gap: 20px;
  margin-bottom: 20px;
}

.skeleton-test-title {
  height: 22px;
  background: linear-gradient(
    90deg,
    rgba(96, 165, 250, 0.1) 0%,
    rgba(96, 165, 250, 0.18) 50%,
    rgba(96, 165, 250, 0.1) 100%
  );
  border-radius: 10px;
  flex: 1;
  max-width: 450px;
  animation: shimmer 2.5s ease-in-out infinite;
  background-size: 200% 100%;
  background-position: 200% 0;
}

.skeleton-test-badge {
  height: 22px;
  width: 100px;
  background: linear-gradient(
    90deg,
    rgba(96, 165, 250, 0.1) 0%,
    rgba(96, 165, 250, 0.18) 50%,
    rgba(96, 165, 250, 0.1) 100%
  );
  border-radius: 10px;
  animation: shimmer 2.5s ease-in-out infinite;
  background-size: 200% 100%;
  background-position: 200% 0;
}

.skeleton-test-meta {
  display: flex;
  gap: 28px;
  margin-bottom: 20px;
  padding: 20px 0;
  border-top: 1px solid rgba(96, 165, 250, 0.08);
  border-bottom: 1px solid rgba(96, 165, 250, 0.08);
}

.skeleton-meta-item {
  height: 18px;
  background: linear-gradient(
    90deg,
    rgba(96, 165, 250, 0.08) 0%,
    rgba(96, 165, 250, 0.15) 50%,
    rgba(96, 165, 250, 0.08) 100%
  );
  border-radius: 8px;
  flex: 1;
  max-width: 120px;
  animation: shimmer 2.5s ease-in-out infinite;
  background-size: 200% 100%;
  background-position: 200% 0;
}

.skeleton-test-actions {
  display: flex;
  gap: 16px;
}

.skeleton-action-btn {
  height: 44px;
  background: linear-gradient(
    90deg,
    rgba(96, 165, 250, 0.1) 0%,
    rgba(96, 165, 250, 0.18) 50%,
    rgba(96, 165, 250, 0.1) 100%
  );
  border-radius: 10px;
  flex: 1;
  max-width: 130px;
  animation: shimmer 2.5s ease-in-out infinite;
  background-size: 200% 100%;
  background-position: 200% 0;
}

/* Responsive - Tablet */
@media (max-width: 1024px) {
  .skeleton-stats {
    grid-template-columns: repeat(3, 1fr);
    gap: 16px;
  }

  .skeleton-test-item {
    padding: 20px;
  }

  .skeleton-test-meta {
    gap: 16px;
  }
}

/* Responsive - Small Tablet */
@media (max-width: 768px) {
  .skeleton-stats {
    grid-template-columns: repeat(2, 1fr);
    gap: 16px;
    margin-bottom: 32px;
  }

  .skeleton-title {
    width: 70%;
  }

  .skeleton-subtitle {
    width: 85%;
  }

  .skeleton-test-header {
    flex-direction: column;
    align-items: flex-start;
  }

  .skeleton-test-badge {
    width: 100%;
    max-width: none;
  }

  .skeleton-test-actions {
    flex-wrap: wrap;
  }

  .skeleton-action-btn {
    max-width: calc(33% - 8px);
  }
}

/* Responsive - Mobile */
@media (max-width: 480px) {
  .dashboard-container {
    padding: 32px 16px 60px;
  }

  .skeleton-loader {
    margin: 0 auto;
  }

  .skeleton-header {
    margin-bottom: 32px;
  }

  .skeleton-stats {
    grid-template-columns: 1fr;
    margin-bottom: 32px;
  }

  .skeleton-title {
    width: 85%;
  }

  .skeleton-subtitle {
    width: 95%;
  }

  .skeleton-test-item {
    padding: 16px;
  }

  .skeleton-test-meta {
    flex-wrap: wrap;
    gap: 12px;
    padding: 12px 0;
  }

  .skeleton-meta-item {
    flex: 1;
    min-width: calc(50% - 6px);
  }

  .skeleton-action-btn {
    max-width: calc(33% - 8px);
    height: 36px;
  }
}

/* ==================== SKELETON LOADER - PROFILE & HISTORY ==================== */

/* Profile Skeleton */
.skeleton-profile-container {
  max-width: 1200px;
  margin: 0 auto;
  padding: 48px 20px;
  animation: fadeIn 0.4s ease-out;
}

.skeleton-profile-card {
  background: linear-gradient(135deg, 
    rgba(30, 58, 138, 0.25) 0%, 
    rgba(30, 58, 138, 0.15) 100%);
  border: 1px solid rgba(96, 165, 250, 0.1);
  border-radius: 20px;
  padding: 48px;
  margin-bottom: 40px;
  animation: fadeIn 0.4s ease-out 0.1s both;
  position: relative;
  overflow: hidden;
}

.skeleton-profile-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: linear-gradient(
    90deg,
    rgba(96, 165, 250, 0.08) 0%,
    rgba(96, 165, 250, 0.15) 50%,
    rgba(96, 165, 250, 0.08) 100%
  );
  animation: shimmer 2s ease-in-out infinite;
  background-size: 200% 100%;
  background-position: 200% 0;
}

.skeleton-profile-avatar {
  width: 120px;
  height: 120px;
  border-radius: 50%;
  background: linear-gradient(
    90deg,
    rgba(96, 165, 250, 0.08) 0%,
    rgba(96, 165, 250, 0.15) 50%,
    rgba(96, 165, 250, 0.08) 100%
  );
  margin: 0 auto 24px;
  animation: shimmer 2s ease-in-out infinite;
  background-size: 200% 100%;
  background-position: 200% 0;
}

.skeleton-profile-name {
  height: 32px;
  background: linear-gradient(
    90deg,
    rgba(96, 165, 250, 0.08) 0%,
    rgba(96, 165, 250, 0.15) 50%,
    rgba(96, 165, 250, 0.08) 100%
  );
  border-radius: 8px;
  margin: 0 auto 12px;
  max-width: 300px;
  animation: shimmer 2s ease-in-out infinite;
  background-size: 200% 100%;
  background-position: 200% 0;
}

.skeleton-profile-handle {
  height: 16px;
  background: linear-gradient(
    90deg,
    rgba(96, 165, 250, 0.08) 0%,
    rgba(96, 165, 250, 0.12) 50%,
    rgba(96, 165, 250, 0.08) 100%
  );
  border-radius: 6px;
  margin: 0 auto 24px;
  max-width: 200px;
  animation: shimmer 2s ease-in-out infinite;
  background-size: 200% 100%;
  background-position: 200% 0;
}

.skeleton-profile-grid {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 24px;
  margin-bottom: 32px;
  animation: fadeIn 0.4s ease-out 0.15s both;
}

.skeleton-profile-item {
  height: 60px;
  background: linear-gradient(
    90deg,
    rgba(96, 165, 250, 0.08) 0%,
    rgba(96, 165, 250, 0.15) 50%,
    rgba(96, 165, 250, 0.08) 100%
  );
  border-radius: 12px;
  animation: shimmer 2s ease-in-out infinite;
  background-size: 200% 100%;
  background-position: 200% 0;
}

.skeleton-profile-stats {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 20px;
  animation: fadeIn 0.4s ease-out 0.2s both;
}

.skeleton-profile-stat-card {
  background: linear-gradient(135deg, 
    rgba(30, 58, 138, 0.25) 0%, 
    rgba(30, 58, 138, 0.15) 100%);
  border: 1px solid rgba(96, 165, 250, 0.1);
  border-radius: 14px;
  padding: 20px;
  height: 100px;
  animation: shimmer 2s ease-in-out infinite;
  background-size: 200% 100%;
  background-position: 200% 0;
  position: relative;
  overflow: hidden;
}

.skeleton-profile-stat-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: linear-gradient(
    90deg,
    rgba(96, 165, 250, 0.08) 0%,
    rgba(96, 165, 250, 0.15) 50%,
    rgba(96, 165, 250, 0.08) 100%
  );
  animation: shimmer 2s ease-in-out infinite;
  background-size: 200% 100%;
  background-position: 200% 0;
}

/* History Skeleton */
.skeleton-history-container {
  max-width: 1200px;
  margin: 0 auto;
  padding: 48px 20px;
  animation: fadeIn 0.4s ease-out;
}

.skeleton-history-header {
  height: 36px;
  background: linear-gradient(
    90deg,
    rgba(96, 165, 250, 0.08) 0%,
    rgba(96, 165, 250, 0.15) 50%,
    rgba(96, 165, 250, 0.08) 100%
  );
  border-radius: 10px;
  margin-bottom: 40px;
  width: 200px;
  animation: shimmer 2s ease-in-out infinite;
  background-size: 200% 100%;
  background-position: 200% 0;
}

.skeleton-history-section {
  background: linear-gradient(135deg, 
    rgba(30, 58, 138, 0.25) 0%, 
    rgba(30, 58, 138, 0.15) 100%);
  border: 1px solid rgba(96, 165, 250, 0.1);
  border-radius: 16px;
  padding: 28px;
  margin-bottom: 40px;
  animation: fadeIn 0.4s ease-out 0.1s both;
}

.skeleton-history-title {
  height: 24px;
  background: linear-gradient(
    90deg,
    rgba(96, 165, 250, 0.08) 0%,
    rgba(96, 165, 250, 0.15) 50%,
    rgba(96, 165, 250, 0.08) 100%
  );
  border-radius: 8px;
  margin-bottom: 24px;
  width: 150px;
  animation: shimmer 2s ease-in-out infinite;
  background-size: 200% 100%;
  background-position: 200% 0;
}

.skeleton-history-table {
  display: flex;
  flex-direction: column;
  gap: 12px;
  animation: fadeIn 0.4s ease-out 0.15s both;
}

.skeleton-history-row {
  height: 56px;
  background: linear-gradient(
    90deg,
    rgba(96, 165, 250, 0.08) 0%,
    rgba(96, 165, 250, 0.15) 50%,
    rgba(96, 165, 250, 0.08) 100%
  );
  border-radius: 10px;
  animation: shimmer 2s ease-in-out infinite;
  background-size: 200% 100%;
  background-position: 200% 0;
  position: relative;
  overflow: hidden;
}

.skeleton-history-row::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: linear-gradient(
    90deg,
    rgba(96, 165, 250, 0.08) 0%,
    rgba(96, 165, 250, 0.15) 50%,
    rgba(96, 165, 250, 0.08) 100%
  );
  animation: shimmer 2s ease-in-out infinite;
  background-size: 200% 100%;
  background-position: 200% 0;
}

/* Responsive - History Skeleton */
@media (max-width: 768px) {
  .skeleton-profile-grid {
    grid-template-columns: 1fr;
    gap: 16px;
  }

  .skeleton-profile-stats {
    grid-template-columns: repeat(2, 1fr);
    gap: 16px;
  }

  .skeleton-profile-card {
    padding: 32px;
  }

  .skeleton-history-container {
    padding: 32px 16px;
  }

  .skeleton-history-section {
    padding: 20px;
  }
}

@media (max-width: 480px) {
  .skeleton-profile-stats {
    grid-template-columns: 1fr;
    gap: 12px;
  }

  .skeleton-profile-container {
    padding: 24px 12px;
  }

  .skeleton-profile-card {
    padding: 24px;
    margin-bottom: 28px;
  }

  .skeleton-history-container {
    padding: 24px 12px;
  }

  .skeleton-history-section {
    padding: 16px;
    margin-bottom: 24px;
  }

  .skeleton-history-row {
    height: 48px;
  }
}

/* ============================================
   LIGHT THEME - Dashboard Page
   ============================================ */

:root[data-theme='light'] .dashboard-container {
  background: linear-gradient(180deg, #f8fafc 0%, #f1f5f9 50%, #f8fafc 100%);
}

:root[data-theme='light'] .dashboard-header {
  background: linear-gradient(135deg, rgba(248, 250, 252, 0.9) 0%, rgba(241, 245, 249, 0.9) 100%);
}

:root[data-theme='light'] .dashboard-header h1 {
  color: #0f1728;
}

:root[data-theme='light'] .dashboard-header p {
  color: #475569;
}

:root[data-theme='light'] .dashboard-stats {
  background: linear-gradient(135deg, rgba(255, 255, 255, 0.8) 0%, rgba(248, 250, 252, 0.8) 100%);
  border-color: rgba(226, 232, 240, 0.6);
  box-shadow: 0 4px 12px rgba(100, 116, 139, 0.08);
}

:root[data-theme='light'] .stat-card {
  background: linear-gradient(135deg, rgba(255, 255, 255, 0.8) 0%, rgba(248, 250, 252, 0.8) 100%);
  border-color: rgba(226, 232, 240, 0.6);
  box-shadow: 0 4px 12px rgba(100, 116, 139, 0.08);
}

:root[data-theme='light'] .stat-card:hover {
  background: linear-gradient(135deg, rgba(255, 255, 255, 0.95) 0%, rgba(248, 250, 252, 0.95) 100%);
  border-color: rgba(203, 213, 225, 0.8);
}

:root[data-theme='light'] .stat-card-label {
  color: #64748b;
}

:root[data-theme='light'] .stat-card-value {
  color: #0f1728;
}

:root[data-theme='light'] .stat-card-subtitle {
  color: #94a3b8;
}

:root[data-theme='light'] .dashboard-cta {
  background: linear-gradient(135deg, #2563eb 0%, #1d4ed8 100%);
  box-shadow: 0 8px 24px rgba(37, 99, 235, 0.2);
}

:root[data-theme='light'] .dashboard-cta:hover {
  background: linear-gradient(135deg, #1d4ed8 0%, #1e40af 100%);
  box-shadow: 0 12px 32px rgba(37, 99, 235, 0.3);
}

:root[data-theme='light'] .empty-state {
  background: transparent;
}

:root[data-theme='light'] .empty-icon {
  background-color: rgba(226, 232, 240, 0.5);
  border-color: rgba(203, 213, 225, 0.6);
  box-shadow: 0 4px 12px rgba(100, 116, 139, 0.1);
}

:root[data-theme='light'] .empty-state h2 {
  color: #0f1728;
}

:root[data-theme='light'] .empty-state p {
  color: #475569;
}

:root[data-theme='light'] .empty-cta {
  background: linear-gradient(135deg, #2563eb 0%, #1d4ed8 100%);
  border-color: rgba(37, 99, 235, 0.3);
  box-shadow: 0 4px 12px rgba(37, 99, 235, 0.2);
}

:root[data-theme='light'] .empty-cta:hover {
  background: linear-gradient(135deg, #1d4ed8 0%, #1e40af 100%);
  box-shadow: 0 8px 20px rgba(37, 99, 235, 0.3);
}

:root[data-theme='light'] .tests-container {
  background: transparent;
}

:root[data-theme='light'] .test-item {
  background: linear-gradient(135deg, rgba(255, 255, 255, 0.8) 0%, rgba(248, 250, 252, 0.8) 100%);
  border-color: rgba(226, 232, 240, 0.6);
  box-shadow: 0 4px 12px rgba(100, 116, 139, 0.08);
}

:root[data-theme='light'] .test-item:hover {
  background: linear-gradient(135deg, rgba(255, 255, 255, 0.95) 0%, rgba(248, 250, 252, 0.95) 100%);
  border-color: rgba(203, 213, 225, 0.8);
  box-shadow: 0 8px 20px rgba(100, 116, 139, 0.12);
}

:root[data-theme='light'] .test-item-title {
  color: #0f1728;
}

:root[data-theme='light'] .test-item-topic {
  color: #475569;
}

:root[data-theme='light'] .meta-label {
  color: #64748b;
}

:root[data-theme='light'] .meta-value {
  color: #0f1728;
}

:root[data-theme='light'] .badge-published {
  background: linear-gradient(135deg, rgba(16, 185, 129, 0.15) 0%, rgba(5, 150, 105, 0.15) 100%);
  color: #059669;
  border-color: rgba(16, 185, 129, 0.3);
}

:root[data-theme='light'] .badge-draft {
  background: linear-gradient(135deg, rgba(156, 163, 175, 0.15) 0%, rgba(107, 114, 128, 0.15) 100%);
  color: #4b5563;
  border-color: rgba(107, 114, 128, 0.3);
}

:root[data-theme='light'] .test-action-btn {
  background: linear-gradient(135deg, #2563eb 0%, #1d4ed8 100%);
  border-color: rgba(37, 99, 235, 0.3);
  color: white;
}

:root[data-theme='light'] .test-action-btn:hover {
  background: linear-gradient(135deg, #1d4ed8 0%, #1e40af 100%);
  box-shadow: 0 4px 12px rgba(37, 99, 235, 0.2);
}

:root[data-theme='light'] .test-action-btn.delete-btn {
  background: linear-gradient(135deg, #dc2626 0%, #b91c1c 100%);
  border-color: rgba(220, 38, 38, 0.3);
}

:root[data-theme='light'] .test-action-btn.delete-btn:hover {
  background: linear-gradient(135deg, #b91c1c 0%, #991b1b 100%);
  box-shadow: 0 4px 12px rgba(220, 38, 38, 0.2);
}

:root[data-theme='light'] .smart-tips-container {
  background: linear-gradient(135deg, rgba(255, 255, 255, 0.9) 0%, rgba(248, 250, 252, 0.9) 100%);
  border-color: rgba(226, 232, 240, 0.6);
  box-shadow: 0 4px 12px rgba(100, 116, 139, 0.1);
}

:root[data-theme='light'] .smart-tips-title {
  color: #0f1728;
}

:root[data-theme='light'] .smart-tips-content {
  color: #475569;
}