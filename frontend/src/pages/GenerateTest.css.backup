.generate-container {
  padding: 40px 20px;
  max-width: 1000px;
}

.generate-header {
  text-align: center;
  margin-bottom: 40px;
}

.generate-header h1 {
  font-size: 36px;
  color: #e2e8f0;
  margin-bottom: 10px;
}

.generate-header p {
  color: #94a3b8;
  font-size: 18px;
}

.generate-content {
  display: grid;
  grid-template-columns: 1.5fr 1fr;
  gap: 32px;
}

.generate-form-card {
  padding: 32px;
}

.input-group small {
  display: block;
  color: #64748b;
  font-size: 13px;
  margin-top: 8px;
  font-weight: 500;
  letter-spacing: 0.2px;
}

.input-group {
  margin-bottom: 24px;
  display: flex;
  flex-direction: column;
  gap: 8px;
}

.input-group label {
  font-size: 15px;
  font-weight: 600;
  color: #f1f5f9;
  display: flex;
  align-items: center;
  gap: 6px;
  letter-spacing: 0.3px;
  text-transform: capitalize;
}

.input-group input,
.input-group select {
  padding: 12px 16px;
  background: linear-gradient(135deg, rgba(15, 23, 42, 0.6) 0%, rgba(30, 41, 59, 0.4) 100%);
  border: 1.5px solid rgba(96, 165, 250, 0.2);
  border-radius: 10px;
  color: #e2e8f0;
  font-family: 'Segoe UI', Roboto, sans-serif;
  font-size: 14px;
  line-height: 1.5;
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  outline: none;
}

.input-group input::placeholder,
.input-group select {
  color: #64748b;
  font-size: 15px;
  font-weight: 400;
  letter-spacing: 0.2px;
}

.input-group input:focus,
.input-group select:focus {
  background: linear-gradient(135deg, rgba(15, 23, 42, 0.8) 0%, rgba(30, 41, 59, 0.6) 100%);
  border-color: rgba(96, 165, 250, 0.5);
  box-shadow: 0 0 0 3px rgba(96, 165, 250, 0.1), 
              0 8px 24px rgba(96, 165, 250, 0.1);
}

.test-code-display {
  background: #0f172a;
  padding: 20px;
  border-radius: 8px;
  margin: 16px 0;
  text-align: center;
}

.test-code-display span {
  display: block;
  color: #94a3b8;
  margin-bottom: 8px;
}

.test-code-display strong {
  color: #3b82f6;
  font-size: 32px;
  letter-spacing: 3px;
  font-family: monospace;
}

.info-card {
  background: #1e293b;
  padding: 24px;
  height: fit-content;
}

.info-card h3 {
  color: #e2e8f0;
  margin-bottom: 20px;
  font-size: 24px;
}

.info-list {
  list-style: none;
  padding: 0;
  margin-bottom: 32px;
}

.info-list li {
  padding: 16px;
  background: #0f172a;
  border-radius: 8px;
  margin-bottom: 12px;
  color: #cbd5e1;
  border-left: 3px solid #3b82f6;
}

.info-list strong {
  color: #e2e8f0;
}

.tips {
  background: #0f172a;
  padding: 20px;
  border-radius: 8px;
  border-left: 3px solid #10b981;
}

.tips h4 {
  color: #10b981;
  margin-bottom: 12px;
}

.tips ul {
  list-style: none;
  padding: 0;
}

.tips li {
  padding: 8px 0;
  color: #94a3b8;
  position: relative;
  padding-left: 24px;
}

.tips li:before {
  content: '✓';
  position: absolute;
  left: 0;
  color: #10b981;
  font-weight: bold;
}

@media (max-width: 768px) {
  .generate-content {
    grid-template-columns: 1fr;
  }
}
/* Add these styles to your existing GenerateTest.css */

.success-message {
  background: linear-gradient(135deg, #10b981 0%, #059669 100%);
  color: white;
  padding: 20px;
  border-radius: 12px;
  margin-bottom: 20px;
  animation: slideIn 0.5s ease;
}

@keyframes slideIn {
  from {
    opacity: 0;
    transform: translateY(-10px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

.success-message strong {
  display: block;
  font-size: 1.2em;
  margin-bottom: 15px;
}

.test-code-display {
  background: rgba(255, 255, 255, 0.15);
  padding: 15px;
  border-radius: 8px;
  margin: 15px 0;
  backdrop-filter: blur(10px);
}

.test-code-label {
  font-size: 0.9em;
  margin-bottom: 8px;
  opacity: 0.9;
}

.test-code-value {
  display: flex;
  align-items: center;
  gap: 10px;
}

.test-code-value code {
  flex: 1;
  background: rgba(0, 0, 0, 0.2);
  padding: 12px 15px;
  border-radius: 6px;
  font-family: 'Courier New', monospace;
  font-size: 1.1em;
  letter-spacing: 1px;
  word-break: break-all;
}

.btn-copy {
  background: rgba(255, 255, 255, 0.25);
  border: 1px solid rgba(255, 255, 255, 0.3);
  color: white;
  padding: 10px 15px;
  border-radius: 6px;
  cursor: pointer;
  font-size: 0.9em;
  transition: all 0.3s ease;
  white-space: nowrap;
}

.btn-copy:hover {
  background: rgba(255, 255, 255, 0.35);
  transform: translateY(-2px);
}

.test-details {
  margin: 15px 0;
  padding: 15px;
  background: rgba(255, 255, 255, 0.1);
  border-radius: 8px;
}

.test-details p {
  margin: 8px 0;
  display: flex;
  justify-content: space-between;
}

.test-details strong {
  color: rgba(255, 255, 255, 0.9);
}

.share-instruction {
  margin: 15px 0;
  text-align: center;
  font-size: 0.95em;
  opacity: 0.95;
}

.action-buttons {
  display: flex;
  gap: 10px;
  margin-top: 15px;
}

.action-buttons .btn {
  flex: 1;
  padding: 12px;
  font-size: 1em;
  border: none;
  cursor: pointer;
  border-radius: 8px;
  transition: all 0.3s ease;
}

.action-buttons .btn-secondary {
  background: rgba(255, 255, 255, 0.2);
  color: white;
}

.action-buttons .btn-secondary:hover {
  background: rgba(255, 255, 255, 0.3);
  transform: translateY(-2px);
}

.action-buttons .btn-primary {
  background: white;
  color: #10b981;
  font-weight: 600;
}

.action-buttons .btn-primary:hover {
  background: #f0f0f0;
  transform: translateY(-2px);
}

.error-message {
  background: linear-gradient(135deg, #ef4444 0%, #dc2626 100%);
  color: white;
  padding: 15px;
  border-radius: 8px;
  margin-bottom: 15px;
  animation: shake 0.5s ease;
}

@keyframes shake {
  0%, 100% { transform: translateX(0); }
  25% { transform: translateX(-10px); }
  75% { transform: translateX(10px); }
}

.loading-spinner {
  display: inline-block;
  width: 16px;
  height: 16px;
  border: 2px solid rgba(255, 255, 255, 0.3);
  border-top-color: white;
  border-radius: 50%;
  animation: spin 0.8s linear infinite;
  margin-right: 8px;
}

@keyframes spin {
  to { transform: rotate(360deg); }
}

input:disabled,
select:disabled {
  opacity: 0.6;
  cursor: not-allowed;
}

/* Skeleton loader styles used by analytics overview */
.animate-pulse {
  animation: pulse 1.2s ease-in-out infinite;
}

@keyframes pulse {
  0% { opacity: 1; }
  50% { opacity: 0.5; }
  100% { opacity: 1; }
}

.bg-slate-800\/30 { background-color: rgba(15, 23, 42, 0.3); }
.bg-slate-700 { background-color: #0f172a; }
.rounded { border-radius: 8px; }
.h-6 { height: 1.5rem; }
.h-4 { height: 1rem; }
.mb-4 { margin-bottom: 1rem; }
.mb-3 { margin-bottom: 0.75rem; }
.p-6 { padding: 1.5rem; }
.grid { display: grid; }
.grid-cols-4 { grid-template-columns: repeat(4, minmax(0,1fr)); }
.grid-cols-2 { grid-template-columns: repeat(2, minmax(0,1fr)); }
.grid-cols-1 { grid-template-columns: repeat(1, minmax(0,1fr)); }
.md\:grid-cols-4 { grid-template-columns: repeat(4, minmax(0,1fr)); }
.lg\:grid-cols-2 { grid-template-columns: repeat(2, minmax(0,1fr)); }
.gap-4 { gap: 1rem; }
.gap-3 { gap: 0.75rem; }
.space-y-4 > * + * { margin-top: 1rem; }
.space-y-6 > * + * { margin-top: 1.5rem; }
.text-slate-400 { color: #94a3b8; }
.text-slate-200 { color: #e6eef8; }
.text-white { color: #fff; }
.font-extrabold { font-weight: 800; }
.text-3xl { font-size: 1.875rem; }
.text-lg { font-size: 1.125rem; }
.rounded-full { border-radius: 9999px; }
.h-3 { height: 0.75rem; }
.pb-4 { padding-bottom: 1rem; }
.border-b { border-bottom: 1px solid rgba(148,163,184,0.08); }
.last\:border-0:last-child { border: 0; }

/* ==================== PREMIUM PASTE CONTENT SECTION ==================== */

.paste-content-group {
  position: relative;
}

.label-wrapper {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 12px;
}

.paste-content-group label {
  font-size: 15px;
  font-weight: 600;
  color: #f1f5f9;
  display: flex;
  align-items: center;
  gap: 8px;
  letter-spacing: 0.3px;
}

.label-badge {
  display: inline-block;
  background: linear-gradient(135deg, rgba(96, 165, 250, 0.2) 0%, rgba(96, 165, 250, 0.1) 100%);
  color: #60a5fa;
  padding: 2px 8px;
  border-radius: 6px;
  font-size: 12px;
  font-weight: 500;
  border: 1px solid rgba(96, 165, 250, 0.2);
}

.char-count {
  font-size: 12px;
  font-weight: 600;
  padding: 4px 10px;
  border-radius: 6px;
  background: rgba(148, 163, 184, 0.1);
  transition: all 0.2s ease;
}

.premium-textarea {
  width: 100%;
  padding: 16px;
  background: linear-gradient(135deg, rgba(15, 23, 42, 0.6) 0%, rgba(30, 41, 59, 0.4) 100%);
  border: 1.5px solid rgba(96, 165, 250, 0.2);
  border-radius: 12px;
  color: #e2e8f0;
  font-family: 'Segoe UI', Roboto, sans-serif;
  font-size: 14px;
  line-height: 1.6;
  resize: vertical;
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  min-height: 140px;
}

.premium-textarea::placeholder {
  color: #64748b;
  font-size: 15px;
  font-weight: 400;
  letter-spacing: 0.2px;
}

.premium-textarea:focus {
  outline: none;
  background: linear-gradient(135deg, rgba(15, 23, 42, 0.8) 0%, rgba(30, 41, 59, 0.6) 100%);
  border-color: rgba(96, 165, 250, 0.5);
  box-shadow: 0 0 0 3px rgba(96, 165, 250, 0.1), 
              0 8px 24px rgba(96, 165, 250, 0.1);
}

.premium-textarea:disabled {
  opacity: 0.5;
  cursor: not-allowed;
  background: linear-gradient(135deg, rgba(15, 23, 42, 0.4) 0%, rgba(30, 41, 59, 0.2) 100%);
}

/* ============================================
   LIGHT THEME - GenerateTest Page
   ============================================ */

:root[data-theme='light'] .generate-container {
  background: linear-gradient(180deg, #f8fafc 0%, #f1f5f9 50%, #f8fafc 100%);
}

:root[data-theme='light'] .generation-card {
  background: linear-gradient(135deg, rgba(255, 255, 255, 0.9) 0%, rgba(248, 250, 252, 0.9) 100%);
  border-color: rgba(226, 232, 240, 0.8);
  box-shadow: 0 8px 24px rgba(100, 116, 139, 0.1);
}

:root[data-theme='light'] .form-group label {
  color: #0f1728;
}

:root[data-theme='light'] .form-group .description {
  color: #475569;
}

:root[data-theme='light'] .premium-input,
:root[data-theme='light'] .premium-select,
:root[data-theme='light'] .premium-textarea {
  background: linear-gradient(135deg, rgba(248, 250, 252, 0.8) 0%, rgba(241, 245, 249, 0.8) 100%);
  border-color: rgba(203, 213, 225, 0.5);
  color: #0f1728;
}

:root[data-theme='light'] .premium-input::placeholder,
:root[data-theme='light'] .premium-select::placeholder,
:root[data-theme='light'] .premium-textarea::placeholder {
  color: #94a3b8;
}

:root[data-theme='light'] .premium-input:focus,
:root[data-theme='light'] .premium-select:focus,
:root[data-theme='light'] .premium-textarea:focus {
  background: linear-gradient(135deg, rgba(255, 255, 255, 0.95) 0%, rgba(248, 250, 252, 0.95) 100%);
  border-color: rgba(37, 99, 235, 0.5);
  box-shadow: 0 0 0 3px rgba(37, 99, 235, 0.1), 0 8px 24px rgba(37, 99, 235, 0.1);
}

:root[data-theme='light'] .char-count {
  background: rgba(203, 213, 225, 0.1);
  color: #64748b;
}

:root[data-theme='light'] .cta-btn-primary {
  background: linear-gradient(135deg, #2563eb 0%, #1d4ed8 100%);
  box-shadow: 0 8px 24px rgba(37, 99, 235, 0.2);
}

:root[data-theme='light'] .cta-btn-primary:hover:not(:disabled) {
  box-shadow: 0 12px 32px rgba(37, 99, 235, 0.3);
}

:root[data-theme='light'] .error-message {
  color: #dc2626;
}

:root[data-theme='light'] .success-message {
  color: #059669;
}

.char-indicator {
  margin-top: 12px;
  display: flex;
  flex-direction: column;
  gap: 8px;
}

.indicator-bar {
  height: 6px;
  background: rgba(96, 165, 250, 0.1);
  border-radius: 3px;
  overflow: hidden;
  border: 1px solid rgba(96, 165, 250, 0.15);
}

.indicator-fill {
  height: 100%;
  border-radius: 3px;
  transition: all 0.3s ease;
  box-shadow: 0 0 12px currentColor;
}

.char-hint {
  font-size: 13px;
  color: #94a3b8;
  display: flex;
  align-items: center;
  gap: 6px;
  transition: all 0.2s ease;
}

.premium-textarea:focus ~ .char-indicator .char-hint {
  color: #cbd5e1;
}

/* Responsive - Tablet */
@media (max-width: 768px) {
  .premium-textarea {
    font-size: 16px;
    padding: 14px;
    min-height: 120px;
  }

  .label-wrapper {
    flex-direction: column;
    align-items: flex-start;
    gap: 6px;
  }

  .char-count {
    align-self: flex-end;
  }
}

/* Responsive - Mobile */
@media (max-width: 480px) {
  .premium-textarea {
    font-size: 16px;
    padding: 12px;
    min-height: 100px;
    border-radius: 8px;
  }

  .label-badge {
    font-size: 11px;
    padding: 2px 6px;
  }

  .char-hint {
    font-size: 12px;
  }
}